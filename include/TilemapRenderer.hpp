#pragma once 
#include <vector>
#include <SDL2/SDL.h>
#include <GlobalsStructs.hpp>

class TilemapRenderer
{
    public:
        TilemapRenderer(camera *camera , );
    private:
        camera *camera;
};